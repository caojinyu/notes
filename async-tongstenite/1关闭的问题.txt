因为这个websocket是一个客户端和服务端统一的,所以两个必须是配合使用的.

shutdown是linux的api,真实的操作套接字的.shutdown函数不能关闭套接字，只能关闭输入和输出流，然后发送EOF，假设套接字为A，那么这个函数会关闭所有和A相关的套接字，包括复制的；而close能直接关闭套接字。


shutdown如果关于的是Write,那么客户端不能接受数据,如果接受的话,会发生错误,这个错误直接导致套接字流的全部关闭.
